# مواصفات VSK المستقلة — الإصدار 0.2

## المبدأ

VSK لغة مستقلة في **قواعدها، كلماتها، نموذجها التنفيذي، ورسائلها**. لا تُعرّف بوصفها نسخة من Python أو Go، ولا تعتمد على runtime خاص بهما. وجود bootstrap مكتوب بلغة C لا يجعل VSK فرعًا من C؛ C هنا مجرد أداة أولى لبناء المترجم، كما تُستخدم لغة التجميع أو أي لغة مضيفة في المراحل الأولى لأي لغة جديدة.

## الكلمات الأساسية

| الكلمة | الوظيفة |
|---|---|
| `set` | إنشاء أو تحديث متغير |
| `say` | طباعة قيمة إلى الخرج |
| `bring` | طلب مكتبة |
| `when` | شرط |
| `otherwise` | الفرع البديل |
| `repeat` | حلقة شرطية |
| `proc` | تعريف إجراء/دالة |
| `give` | إعادة قيمة من الإجراء |
| `true`, `false`, `nil` | قيم منطقية وفارغة |
| `and`, `or`, `not` | منطق |

## مثال VSK مستقل

```vsk
bring Math
bring unused_package

set Name = "VSK"
set score = 10
score += 5

say name
say Math.sqrt(81)

proc greet(person) {
    say "hello " + person
    give 42
}

set result = GREET("developer")
say result

when score >= 15 {
    say "condition works"
} otherwise {
    say "not reached"
}
```

تُهمل المكتبات المستوردة وغير المستخدمة دون تحذير. وإذا طلب السكربت مكتبة غير موجودة، تطبع النواة تنبيهًا فقط وتتابع التنفيذ. الاسم غير المعروف كذلك لا يوقف البرنامج؛ يُعامل كقيمة `nil` مع تنبيه.

## ملاحظة عن الاستقلالية

لا يمكن إنشاء أول مترجم في فراغ كامل؛ يجب أن يعمل bootstrap الأول فوق عتاد أو نظام موجود. لذلك نواة VSK الأولى مكتوبة بلغة C منخفضة المستوى، لكنها لا تستدعي مفسّر Python ولا تتفرع من قواعد Python أو Go. بعد استقرار اللغة يمكن كتابة مترجم VSK بلغة VSK نفسها، وعندها تبدأ مرحلة **self-hosting**.
